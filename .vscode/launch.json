{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug CHIP-8",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build-debug/chip8",
            "args": ["tetris"],
            "cwd": "${workspaceFolder}",
        }
    ]
}
